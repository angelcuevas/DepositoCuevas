<UserControl x:Class="DepositoCuevas.views.DepositoMapa"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DepositoCuevas.views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500"
             
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:juegos="clr-namespace:DepositoCuevas.views.Juegos" xmlns:ubicacion="clr-namespace:DepositoCuevas.views.ubicacion"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal" 
            TextOptions.TextRenderingMode="Auto"        
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
            Grid.Column="0"
            HorizontalAlignment="Left"
            Margin="24,0,0,0"
            ToolTip="Previous Item"
            Command="{Binding MovePrevCommand}"
            Content="{materialDesign:PackIcon Kind=ArrowLeft, Size=24}"
            Style="{StaticResource MaterialDesignToolButton}"
            Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
            materialDesign:RippleAssist.Feedback="{Binding RelativeSource={RelativeSource Self}, Path=Foreground, Converter={StaticResource BrushRoundConverter}}"/>
            <TextBlock
                Grid.Column="1"
                FontSize="30"
                Margin="0,0,0,8"
                Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                VerticalAlignment="Center"
                Text="Mapa" />
            <Grid Grid.Column="2">
                <ComboBox Grid.Row="2" 
              Style="{StaticResource MaterialDesignOutlinedComboBox}"
              materialDesign:HintAssist.Hint="Buscar"
              materialDesign:HintAssist.HintOpacity=".26"
              IsEditable="True"
              Margin="5"
              Visibility="{Binding Path=VisibilityHelper.Mapa}"
                  

            >
                    <ComboBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding searchCodigoCommand}"/>
                        <KeyBinding Key="Tab" Command="{Binding searchCodigoCommand}"/>
                    </ComboBox.InputBindings>
                    <!--<ComboBoxItem
                Content="Apple" />
                        <ComboBoxItem
                Content="Banana" />
                        <ComboBoxItem
                Content="Pear" />
                        <ComboBoxItem
                Content="Orange" />-->
                </ComboBox>
            </Grid>
        </Grid>


        

  

        <Viewbox Grid.Row="3" Stretch="Uniform" Visibility="{Binding Path=VisibilityHelper.Mapa}">
            <Viewbox.RenderTransform>
                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
            </Viewbox.RenderTransform>
                
            <Canvas Grid.Row="4" Name="myCanvas" Width="800" Height="300" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MouseMove="myCanvas_MouseMove">
                <Canvas.LayoutTransform>
                    <ScaleTransform />
                </Canvas.LayoutTransform>
                <TextBlock Text="{Binding Path=TextHelper.ModuloHoverDescripcion}" Canvas.Right="5" Canvas.Top="5" />
            </Canvas>
                
        </Viewbox>

        <Viewbox  Grid.Row="3" Stretch="Uniform" Visibility="{Binding Path=VisibilityHelper.Estanteria}">
            <Viewbox.RenderTransform>
                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
            </Viewbox.RenderTransform>

            <Canvas Grid.Row="4" Name="myCanvasEstanteria" Width="800" Height="300" Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MouseMove="myCanvas_MouseMove">
                <Canvas.LayoutTransform>
                    <ScaleTransform />
                </Canvas.LayoutTransform>
                <TextBlock Text="{Binding Path=TextHelper.BancalHoverDescripcion}" Canvas.Left="5" Canvas.Top="5" />
            </Canvas>

        </Viewbox>

        <ubicacion:UbicacionContenidoView Grid.Row="3" DataContext="{Binding UbicacionContenido}" Visibility="{Binding DataContext.VisibilityHelper.Contenido, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}"/>
        <ubicacion:IngresoDeLinea Grid.Row="3" DataContext="{Binding Path=MovUbicacionVM}" Visibility="{Binding DataContext.VisibilityHelper.IngresoDeLinea, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}"/>


    </Grid>
</UserControl>
